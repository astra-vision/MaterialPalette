# install: conda env create -f deps.yml
name: matpal
channels:
  - pytorch
  - nvidia
dependencies:
  - pytorch==1.13.1
  - torchvision==0.14.1
  - pytorch-cuda=11.7
  - pip
  - pip:
      - lightning==1.8.3
      - diffusers==0.19.3
      - peft==0.5.0
      - opencv_python
      - jsonargparse
      - easydict